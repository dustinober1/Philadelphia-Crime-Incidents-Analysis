# =============================================================================
# Local compose startup (required for first run)
# =============================================================================
# Copy this file to .env, then run:
#   docker compose up -d --build
WEB_PORT=3001
PIPELINE_REFRESH_INTERVAL_SECONDS=900
ADMIN_PASSWORD=change-me
ADMIN_TOKEN_SECRET=change-me-token-secret
FIRESTORE_COLLECTION_QUESTIONS=questions
GOOGLE_CLOUD_PROJECT=local-dev
CORS_ORIGINS=http://localhost:3001,http://localhost:3000,https://philly-crime-explorer.web.app,https://philly-crime-explorer.firebaseapp.com

# =============================================================================
# Local compose runtime budgets (optional tuning)
# =============================================================================
PIPELINE_CPU_LIMIT=1.00
PIPELINE_MEM_LIMIT=1536m
API_CPU_LIMIT=1.00
API_MEM_LIMIT=1024m
WEB_CPU_LIMIT=1.00
WEB_MEM_LIMIT=1024m

# Runtime preset overlays (optional; used by compose_with_runtime_mode helper)
# - .env.runtime.low-power
# - .env.runtime.high-performance

# =============================================================================
# Analysis data API keys (optional; not required for compose startup)
# =============================================================================
# FRED API Key (Federal Reserve Economic Data)
# Purpose: Unemployment rate data (PAPHIL5URN for Philadelphia County)
# Free key: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your_fred_api_key_here

# U.S. Census API Key
# Purpose: ACS indicators (median income, poverty rate, etc.)
# Free key: https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=your_census_api_key_here

# =============================================================================
# Cloud deployment settings (server-side only)
# =============================================================================
# Used by FastAPI on Cloud Run for admin Q&A authentication and CORS.
# These are intentionally separate from the local-first compose contract above.
