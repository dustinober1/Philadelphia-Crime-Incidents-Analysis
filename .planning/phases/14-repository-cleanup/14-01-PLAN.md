---
phase: 14-repository-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements-dev.txt, analysis/__init__.py]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "pyclean package is installed and runnable"
    - "vulture package is installed and runnable"
    - "autoflake package is installed and runnable"
    - "All packages maintain version compatibility with existing dependencies"
  artifacts:
    - path: "requirements-dev.txt"
      provides: "Development dependency specification"
      contains: "vulture>=2.14,autoflake>=2.3.1,pyclean>=3.5.0"
    - path: "analysis/__init__.py"
      provides: "Package exports validation"
      exports: ["classify_crime_category", "extract_temporal_features", "load_data"]
  key_links:
    - from: "requirements-dev.txt"
      to: "pip"
      via: "pip install -r requirements-dev.txt"
      pattern: "vulture.*autoflake.*pyclean"
---

<objective>
Install Python cleanup tools (pyclean, vulture, autoflake) for automated repository maintenance.

Purpose: Establish tooling foundation for Phase 14 cleanup operations. These tools will remove build artifacts, detect dead code, and clean unused imports.

Output: Updated requirements-dev.txt with cleanup tools installed and validated.
</objective>

<execution_context>
@/Users/dustinober/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustinober/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v1.3-ROADMAP.md
@.planning/research/STACK-testing-cleanup.md
</context>

<tasks>

<task type="auto">
  <name>Install cleanup tools in requirements-dev.txt</name>
  <files>requirements-dev.txt</files>
  <action>
    Add cleanup tools to requirements-dev.txt:
    - vulture>=2.14 (dead code detection)
    - autoflake>=2.3.1 (unused import removal)
    - pyclean>=3.5.0 (Python artifact cleanup)

    Append to requirements-dev.txt after existing dev dependencies. Use >= to allow compatible updates while ensuring minimum version.

    Reference: .planning/research/STACK-testing-cleanup.md lines 368-372
  </action>
  <verify>grep -c "vulture\|autoflake\|pyclean" requirements-dev.txt | grep 3</verify>
  <done>requirements-dev.txt contains vulture>=2.14, autoflake>=2.3.1, and pyclean>=3.5.0</done>
</task>

<task type="auto">
  <name>Install and validate cleanup tools</name>
  <files>requirements-dev.txt</files>
  <action>
    Install cleanup tools using pip:
    ```bash
    pip install -r requirements-dev.txt
    ```

    Verify each tool is installed and runnable:
    1. Run `pyclean --version` - should show version like 3.5.0
    2. Run `vulture --version` - should show version like vulture 2.14
    3. Run `autoflake --version` - should show version like 2.3.1

    If any tool fails to install, check for dependency conflicts with existing packages (pytest, black, ruff, mypy) and resolve version constraints.
  </action>
  <verify>pyclean --version && vulture --version && autoflake --version</verify>
  <done>All three cleanup tools installed and version commands return successfully</done>
</task>

<task type="auto">
  <name>Validate package exports remain functional</name>
  <files>analysis/__init__.py</files>
  <action>
    Verify that cleanup tool installation did not break package imports. Test that analysis package exports work correctly:

    ```bash
    python -c "from analysis import classify_crime_category, extract_temporal_features, load_data; print('Exports OK')"
    ```

    This validates that analysis/__init__.py exports are still accessible and no import conflicts were introduced by new packages.
  </action>
  <verify>python -c "from analysis import classify_crime_category, extract_temporal_features, load_data" && echo "Imports successful"</verify>
  <done>All analysis package imports (classify_crime_category, extract_temporal_features, load_data) function correctly</done>
</task>

</tasks>

<verification>
- pyclean, vulture, and autoflake are installed and runnable
- Version commands return valid version numbers
- analysis package imports work correctly
- No dependency conflicts in requirements-dev.txt
</verification>

<success_criteria>
- All three cleanup tools installed successfully
- Tools are functional (version commands work)
- Package exports remain accessible
- requirements-dev.txt updated with version constraints
</success_criteria>

<output>
After completion, create `.planning/phases/14-repository-cleanup/14-01-SUMMARY.md`
</output>
