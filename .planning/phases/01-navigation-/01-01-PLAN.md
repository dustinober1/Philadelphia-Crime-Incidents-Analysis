---
phase: 01-navigation-layout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/layout.tsx
  - web/src/app/globals.css
  - web/src/components/Footer.tsx
autonomous: true
must_haves:
  truths:
    - Site layout remains readable and usable on mobile, tablet, and desktop without horizontal overflow.
    - Header, main content, and footer spacing adapt by viewport while preserving a clear reading rhythm.
    - Footer attribution and external links remain legible and accessible on small touch screens.
  artifacts:
    - path: web/src/app/layout.tsx
      provides: Global responsive shell and landmark structure
      contains: "<main"
    - path: web/src/app/globals.css
      provides: Shared spacing/typography/touch-safe tokens and base styles
      contains: ":root"
    - path: web/src/components/Footer.tsx
      provides: Responsive footer layout and attribution block
      contains: "Source: Philadelphia Police Department"
  key_links:
    - from: web/src/app/layout.tsx
      to: web/src/components/Navbar.tsx
      via: Root layout composition
      pattern: "<Navbar />"
    - from: web/src/app/layout.tsx
      to: web/src/components/Footer.tsx
      via: Root layout composition
      pattern: "<Footer />"
    - from: web/src/app/globals.css
      to: web/src/components/Footer.tsx
      via: Shared utility/style tokens applied in component classes
      pattern: "text-slate|bg-"
---

<objective>
Establish a reliable responsive shell for the web app before navigation-level changes.

Purpose: Create a consistent, readable, and touch-friendly layout baseline across viewport sizes.
Output: Updated global layout/styling primitives and a hardened footer that behave predictably on mobile, tablet, and desktop.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
@.codex/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-navigation-/01-RESEARCH.md
@web/src/app/layout.tsx
@web/src/app/globals.css
@web/src/components/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden root layout landmarks and responsive container behavior</name>
  <files>web/src/app/layout.tsx</files>
  <action>Refactor the root layout so the page shell has explicit accessibility landmarks and mobile-safe spacing. Keep `Navbar` and `Footer` globally mounted, but improve main container sizing and padding strategy so content is readable from narrow phones through large desktops without horizontal clipping. Preserve current route rendering behavior and metadata exports.</action>
  <verify>cd web && npm run lint</verify>
  <done>`layout.tsx` provides a stable responsive shell with clear landmarks and no regressions in page rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade global CSS tokens for readability and touch ergonomics</name>
  <files>web/src/app/globals.css</files>
  <action>Extend `globals.css` with responsive typography/spacing and touch-friendly interaction defaults that fit the existing visual system. Keep the current palette direction, but ensure base styles support comfortable tap targets, focus visibility, and scalable spacing across breakpoints. Do not introduce a new CSS framework or a full design reset.</action>
  <verify>cd web && npm run typecheck</verify>
  <done>Global styles include responsive/touch-safe defaults and compile cleanly with TypeScript tooling.</done>
</task>

<task type="auto">
  <name>Task 3: Make footer responsive and attribution-first</name>
  <files>web/src/components/Footer.tsx</files>
  <action>Update footer layout/classes to avoid cramped text and link collisions on mobile widths while preserving attribution details. Ensure source/update metadata remain prominent and external links remain easy to activate via touch. Keep existing SWR metadata fetching behavior intact.</action>
  <verify>cd web && npm run build</verify>
  <done>Footer content wraps cleanly on small screens, remains readable, and keeps data-source transparency visible.</done>
</task>

</tasks>

<verification>
- Run `cd web && npm run lint`.
- Run `cd web && npm run typecheck`.
- Run `cd web && npm run build`.
- Manual QA: inspect home and map pages at ~390px, ~768px, and ~1280px widths; confirm no horizontal overflow and readable spacing in shell/footer.
</verification>

<success_criteria>
- App shell is consistently readable across mobile/tablet/desktop (NAV-01 foundation).
- Footer remains legible and touch-friendly without visual overlap.
- Layout changes do not break route rendering or build/lint/type checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-navigation-/01-01-SUMMARY.md`.
</output>
