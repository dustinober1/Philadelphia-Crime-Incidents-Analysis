---
phase: 01-data-exploration
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - scripts/data_exploration/run_exploration.py
autonomous: true
must_haves:
  truths:
    - "User can run the exploration script from the project root"
    - "Output provides clear, readable summary of the dataset"
    - "Script handles loading and profiling integration seamlessly"
  artifacts:
    - path: "scripts/data_exploration/run_exploration.py"
      provides: "Entry point for Phase 1"
  key_links:
    - from: "scripts/data_exploration/run_exploration.py"
      to: "src.data.loader"
      via: "import"
    - from: "scripts/data_exploration/run_exploration.py"
      to: "src.analysis.profiler"
      via: "import"
---

<objective>
Create the runner script that integrates loading and profiling.

Purpose: Fulfill DATA-01 by providing the actual command the user runs to explore the data.
Output: An executable script `scripts/data_exploration/run_exploration.py`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/data/loader.py
@src/analysis/profiler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Runner Script</name>
  <files>scripts/data_exploration/run_exploration.py</files>
  <action>
    Create the main execution script.
    - Set up python path to include `src` (if running from root, `src` might typically be reachable, but `sys.path.append` logic or running as module is safer. Prefer standard imports assuming run from root).
    - Import `load_crime_data` from `src.data.loader`.
    - Import `DataProfiler` from `src.analysis.profiler`.
    - Main logic:
      1. Print "Loading data..."
      2. Call loader.
      3. Instantiate Profiler.
      4. Run checks and print results with formatted headers.
  </action>
  <verify>python scripts/data_exploration/run_exploration.py</verify>
  <done>Script runs and outputs analysis to stdout</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Data Exploration Pipeline</what-built>
  <how-to-verify>
    Run: `python scripts/data_exploration/run_exploration.py`
    Check:
    1. Data loads without error.
    2. Missing value counts are displayed.
    3. Data types are listed.
    4. Outlier counts for numerical columns are shown.
  </how-to-verify>
  <resume-signal>Output looks correct and covers all requirements.</resume-signal>
</task>

</tasks>

<verification>
Execute the script against the real data.
</verification>

<success_criteria>
- Script runs successfully on the local machine
- Output covers all DATA-01 to DATA-05 requirements
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-exploration/01-03-SUMMARY.md`
</output>
